--[=[
PetClientController
Handles local pet visuals: follower tweening, idle animations, and UI updates.
]=]

local PetClientController = {}

function PetClientController.init()
    -- TODO: Connect to remotes for equip/unequip and spawn client-side pet models.
end

function PetClientController.runFollowLoop()
    -- TODO: Tween pets around the player with memory-safe updates.
end

return PetClientController
